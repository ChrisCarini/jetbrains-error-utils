package com.chriscarini.jetbrains.diagnostic.reporter.github;

import com.intellij.testFramework.fixtures.BasePlatformTestCase;


public class AuthenticatedGitHubErrorReportSubmitterTest extends BasePlatformTestCase {
    //TODO(ChrisCarini): Figure out how to create a test that includes the GitHub plugin. We need it to register 
    // an account to have our logic work as desired under test. Without it, we'd likely have to do a major refactor 
    // to be able to stub/mock out everything so we can test the basic functionality.
    public void testSTUB() {
        assertTrue(true);
    }
}